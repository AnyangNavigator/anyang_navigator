# 처음 시작하는 팀원용 가이드

전공 지식이 없어도 됩니다. 아래 두 가지만 손으로 직접 하고, 나머지는 Claude Code한테 프롬프트 한 번 붙여넣으면 알아서 진행합니다.

## 1. Claude Code 설치 + 로그인 (직접 해야 하는 부분)

1. https://claude.com/code 접속 → 내 운영체제(Windows/Mac)에 맞는 버전 다운로드 → 설치
2. 실행 후 팀에서 정한 Claude 계정으로 로그인

## 2. 빈 폴더 만들어서 열기 (직접 해야 하는 부분)

1. 바탕화면 등 원하는 곳에 새 폴더를 하나 만듭니다 (이름은 아무거나, 예: `anyang`)
2. Claude Code에서 "폴더 열기(Open Folder)"로 방금 만든 폴더를 엽니다

## 3. 아래 프롬프트를 대화창에 그대로 붙여넣기

**본인 이름 찾아서 그 블록을 통째로 복사 → Claude Code 대화창에 붙여넣기 → Enter.**
중간에 "이 명령 실행해도 될까요?" 같은 확인 창이 뜨면 승인(Allow)을 눌러주면 됩니다.
`gh auth login` 단계에서 브라우저가 뜨고 코드를 입력하라고 하면, 화면에 나온 대로 따라 하면 됩니다.

### 정준기 (JK-hustler)

```
너는 지금부터 내 컴퓨터에 "안양 균형발전 내비게이터" 프로젝트 개발 환경을 처음부터 세팅해주는 역할이야.
나는 비전공자라 각 단계를 몰라도 되게, 네가 확인하고 필요하면 설치까지 진행해줘. 순서대로 해줘:

1. git이 설치돼 있는지 확인해줘 (`git --version`). 없으면 내 운영체제에 맞게 설치해줘
   (Windows는 winget, Mac은 공식 인스톨러나 brew).
2. GitHub CLI(`gh`)가 설치돼 있는지 확인해줘 (`gh --version`). 없으면 마찬가지로 설치해줘.
3. `gh auth status`로 내가 이미 로그인돼 있는지 확인해줘. 로그인 안 돼 있으면 `gh auth login`을
   실행해서 나를 안내해줘 — 웹 브라우저로 로그인하는 방식(HTTPS)으로 진행하고, 코드나 링크가
   나오면 나한테 정확히 알려주고 내가 완료했다고 말할 때까지 기다려줘. 내 GitHub 아이디는
   JK-hustler야.
4. 이 저장소를 지금 폴더에 클론해줘: https://github.com/AnyangNavigator/anyang_navigator.git
5. 클론된 프로젝트 폴더로 들어가서 Python이 설치돼 있는지 확인하고,
   `pip install -r requirements-dev.txt`로 필요한 패키지를 설치해줘.
6. 모든 준비가 끝나면 `/catchup JK-hustler`를 실행해서 내가 할 일을 파악하고 가장 급한 것부터
   시작해줘.

각 단계마다 무슨 일을 했는지 짧게 한국어로 설명해줘.
```

### 이상현 (sangt633-art)

```
너는 지금부터 내 컴퓨터에 "안양 균형발전 내비게이터" 프로젝트 개발 환경을 처음부터 세팅해주는 역할이야.
나는 비전공자라 각 단계를 몰라도 되게, 네가 확인하고 필요하면 설치까지 진행해줘. 순서대로 해줘:

1. git이 설치돼 있는지 확인해줘 (`git --version`). 없으면 내 운영체제에 맞게 설치해줘
   (Windows는 winget, Mac은 공식 인스톨러나 brew).
2. GitHub CLI(`gh`)가 설치돼 있는지 확인해줘 (`gh --version`). 없으면 마찬가지로 설치해줘.
3. `gh auth status`로 내가 이미 로그인돼 있는지 확인해줘. 로그인 안 돼 있으면 `gh auth login`을
   실행해서 나를 안내해줘 — 웹 브라우저로 로그인하는 방식(HTTPS)으로 진행하고, 코드나 링크가
   나오면 나한테 정확히 알려주고 내가 완료했다고 말할 때까지 기다려줘. 내 GitHub 아이디는
   sangt633-art야.
4. 이 저장소를 지금 폴더에 클론해줘: https://github.com/AnyangNavigator/anyang_navigator.git
5. 클론된 프로젝트 폴더로 들어가서 Python이 설치돼 있는지 확인하고,
   `pip install -r requirements-dev.txt`로 필요한 패키지를 설치해줘.
6. 모든 준비가 끝나면 `/catchup sangt633-art`를 실행해서 내가 할 일을 파악하고 가장 급한 것부터
   시작해줘.

각 단계마다 무슨 일을 했는지 짧게 한국어로 설명해줘.
```

## 4. 그다음부터는

새로 컴퓨터를 켜서 작업을 시작할 때마다, Claude Code에서 이 프로젝트 폴더를 열고 아래처럼 자기 아이디로만 입력하면 됩니다.

- 정준기: `/catchup JK-hustler`
- 이상현: `/catchup sangt633-art`
- 김규현: `/catchup k2hop1213`

막히면 에러 메시지를 그대로 복사해서 Claude한테 붙여넣고 물어보세요.
