# PROJECT_SPEC — 안양 균형발전 내비게이터

## 1. 서비스 개요

행정동을 선택하면:
- (A) 그 동이 속한 구(만안구/동안구)의 사회조사 기반 만족도·필요시설 지표 — **구 단위**
- (B) 그 동 자체의 인구 대비 시설밀도 — **동 단위**

를 함께 보여주는 이중 레이어 대시보드. 여기에 "이 지역에 이 시설을 추가하면 격차가 얼마나 줄어드는가"를 계산하는 What-if 시뮬레이터, 그리고 LLM 기반 자연어 리포트·챗봇을 결합한다.

## 2. 사용자 트랙 (두 가지 화면 모드)

### 시민용
1. 동 선택 (드롭다운 또는 지도 클릭)
2. 대시보드 카드: "이 동이 속한 {만안구/동안구}의 사회조사 결과" + "이 동 자체의 시설밀도"
3. "AI 진단 리포트 보기" 버튼 → LLM이 자연어로 설명
4. 챗봇으로 후속 질의 ("옆 동이랑 비교해줘", "복지시설은 어때?")

### 행정용 (What-if 시뮬레이터)
1. 정책 시나리오 선택 (기본 제공: 양지마을 재생사업 / 사용자 정의: 지역+시설유형+예산 직접 입력)
2. 시뮬레이터 실행 → 예상 격차 개선 효과 출력
3. (여력 시) 여러 시나리오를 예산 대비 효과로 순위화

## 3. 데이터 스키마

### `data/population_by_dong.csv`
| 컬럼 | 설명 |
|---|---|
| gu | 만안구 / 동안구 |
| dong | 행정동명 (예: 안양1동) — 총 31개 행정동 |
| total_population | 총인구 |
| male, female | 성별 인구 |
| households | 세대수 |

### `data/survey_11_housing_satisfaction.csv` (구 단위: 안양시 전체/2023/2021/만안구/동안구)
주택·기반시설·주차장 만족도 5단계(매우만족~매우불만족) 비율 + `parking_dissat_total`(약간불만족+매우불만족 합산, 미리 계산됨).

### `data/survey_12_needed_facilities.csv` (구 단위)
9개 시설 유형(국공립어린이집, 공원녹지산책로, 보건의료시설, 사회복지시설, 공영주차시설, 문화예술회관, 도서관, 체육시설, 기타)별 "향후 필요하다" 복수응답 비율.

### `data/survey_16-3_dissatisfaction_reasons.csv` (구 단위)
안양시 거주 불만족 이유 7개 항목 비율 + 전체 불만족 비율(`불만족_total`).

**동-구 매핑**: `population_by_dong.csv`의 `gu` 컬럼으로 어느 동이 어느 구 소속인지 바로 조인 가능.

## 4. What-if 시뮬레이터 로직 (제안)

모델 학습 없이 통계적 추정으로 충분함:

1. `survey_12_needed_facilities.csv`에서 시설 유형별 만안구-동안구 필요도 격차(%p)를 계산 (이미 원본에 있음, 예: 공영주차시설 +15.2%p, 보건의료시설 +18.8%p)
2. 2021→2023→2025 시계열 값(`안양시 전체(2021)`, `(2023)`, `2025` 행)으로 "시설 확충 추세에 따른 필요도 변화율"을 추정 (단순 선형 추세로 충분)
3. 사용자가 "이 지역에 시설 X를 추가"를 입력하면:
   - 해당 시설 유형의 현재 격차(%p) × 추정 개선계수 = 예상 격차 감소폭
   - 개선계수는 하드코딩된 가정값으로 시작해도 무방 (예: 신규 시설 1개소당 필요도 응답 -N%p, 문서에 가정 명시)
4. 기본 데모 시나리오: **양지마을(안양3동, 만안구) 주거재생 혁신지구** — 생활SOC(공영주차시설 등) 신규 공급 → 만안구 주차시설 필요도 격차가 어떻게 줄어드는지 시뮬레이션

이 시뮬레이터는 "정밀한 예측"이 아니라 "정책 우선순위를 논의하기 위한 의사결정 보조 도구"임을 UI 문구로 명시할 것 (과신 방지).

## 5. LLM 활용

- **자동 진단 리포트**: 구조화 지표 + 시뮬레이션 결과를 프롬프트에 넣어 자연어 리포트 생성. 프롬프트에 반드시 "구 단위 통계"와 "동 단위 통계"를 구분해서 서술하라는 규칙 포함 (README의 데이터 주의사항 참고).
- **RAG 챗봇**: `data/*.csv`를 벡터 임베딩하거나, 단순히 구조화 쿼리로 관련 행을 찾아 컨텍스트로 LLM에 전달 (문항 수가 적어서 굳이 복잡한 벡터DB 없이 룰 기반 검색으로도 충분할 수 있음).
- API: Upstage Solar, 네이버 클로바X, OpenAI 등 택1. 별도 파인튜닝 불필요.

## 6. 지도 시각화

카카오맵 또는 브이월드 API로 31개 행정동에 색상 단계(choropleth)로 시설밀도 또는 격차 지수 표시. `population_by_dong.csv`의 동명을 카카오맵 지오코딩 API에 매핑.

## 7. 권장 기술 스택

- 백엔드: FastAPI (Python) — pandas로 CSV 로드, 간단한 인메모리 처리로 충분 (별도 DB 불필요할 수도 있음)
- 프론트엔드: React + Tailwind
- 지도: 카카오맵 JS SDK 또는 react-kakao-maps-sdk
- 배포: 백엔드는 Render/Railway, 프론트는 Vercel

## 8. 개발 우선순위 (MVP 먼저)

1. **1순위 (반드시)**: 대시보드(구 단위 + 동 단위 이중 레이어) + 주차장/보건의료시설 지표
2. **2순위 (반드시)**: What-if 시뮬레이터 (양지마을 시나리오 하나만이라도)
3. **3순위 (반드시)**: LLM 자동 리포트
4. **4순위 (여력 시)**: RAG 챗봇
5. **5순위 (여력 시)**: 예산 대비 효과 스코어링, 지도 시각화 고도화

## 9. 참고 — 대회 규정 관련 제약

- 마감: 2026-09-21. 접수 마감일까지 **시제품이 실제로 작동해야 함** (미완성 프로토타입 감점 요인).
- 3차 발표평가 대상이 되면 발표 PPT 별도 제출 필요.
- 공개검증 단계(수상 후보작 대상, 10일 이상 온라인 공개)가 있으므로 배포된 URL을 유지할 것.
